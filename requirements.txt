# Core Framework
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
jinja2>=3.1.0

# Database (SQLite for dev, MySQL for production)
sqlalchemy>=2.0.25
aiosqlite>=0.19.0
alembic>=1.13.1
# MySQL async driver (pure Python - no C++ build tools needed)
aiomysql>=0.2.0
# For PostgreSQL (install when ready):
# asyncpg==0.29.0
# psycopg2-binary==2.9.9

# Redis & Caching (optional for dev - using in-memory)
# redis==5.0.1
# hiredis==2.3.2

# LLM & AI
openai>=1.12.0
# langchain - install later when needed
# langchain==0.1.6
# langchain-openai==0.0.5
# tiktoken - for token counting (install if needed)

# Vector Store (install later when needed)
# faiss-cpu==1.7.4
# qdrant-client>=1.11.0

# Validation & Settings
pydantic>=2.6.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# HTTP & Async
httpx>=0.26.0
# aiohttp - install if needed for specific integrations

# Utilities
python-dateutil>=2.8.2
pytz>=2024.1
uuid6>=2024.1.12

# OCR (teammate will install when needed)
# easyocr==1.7.1
# pdf2image==1.17.0
# Pillow - install separately if needed: pip install Pillow

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
# pytest-cov==4.1.0

# Dev Tools (install when needed)
# black==24.1.1
# ruff==0.2.0
# mypy==1.8.0
